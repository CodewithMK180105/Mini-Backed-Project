<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Log Out</a>
        </div>
        <h1 class="text-3xl mb-5">Hello, <%= user.name%></h1>
        <h5 class="mb-5">You can create a new post</h5>
        <form method="post" action="/post">
            <textarea class=" p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind?" name="content" id=""></textarea>
            <input class="mt-2 px-3 w-40 py-2 bg-blue-500 block rounded-md cursor-pointer" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <%user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 mb-3 p-4 rounded-md border-2 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
                        <p class="text-sm"><%= post.content %></p>
                        <small class="mt-5 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex mt-5 justify-between">
                            <a class="text-pink-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1? "Like" : "Unlike" %>
                            </a>
                            <a class="text-blue-500" href="">Edit</a>
                        </div>
                    </div>
                <% })%>
            </div>
        </div>
    </div>
</body>
</html>